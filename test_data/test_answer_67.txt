The softmax layer converts the raw output scores (logits) of a model into probabilities that sum to one. Each output node’s probability represents the model’s confidence that the input belongs to the corresponding class.